<h1>Listing users</h1>
<%= will_paginate %>
<p> </p>
<ul>
	<% @users.each do |user| %>
	<li> <%= link_to user.firstName + " " + user.lastName, user %>
		  <% if current_user.userType===1 && !current_user?(user) %>
    | <%= link_to "delete", user, method: :delete,
                                  data: { confirm: "You sure?" } %>
  <% end %>

		 </li>
	
	<% end %>
			</ul>
